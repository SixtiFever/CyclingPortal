package cycling.stage;

import cycling.StageType;

// super class containing all members that each stage must inherit
public class StageSuperclass {

    // Each stage must have a type, points specification, list of teams, segments, start time, results
    public StageType type;

    public int[] points_classification;

    // Static riders_all    --> A static array of rider objects will be constant through every stage


}
